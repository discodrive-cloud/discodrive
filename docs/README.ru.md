<p align="center">
  <img src="img/logo.jpg" alt="DiscoDrive" width="100%">
</p>

# DiscoDrive

[English](../README.md) · [Deutsch](README.de.md) · [Українська](README.uk.md) · [Français](README.fr.md) · [Español](README.es.md) · [**Русский**](README.ru.md) · [Српски](README.sr.md)

**Ваше личное облако. Один сервер, один логин — файлы, календари, контакты, задачи, музыка и книги.**

DiscoDrive — это self-hosted-замена набору подписок и разрозненных утилит, которые обычно приходится склеивать, чтобы уйти с iCloud, Google или Dropbox. Вместо того чтобы держать File Browser, Radicale, Subsonic-сервер *и* Calibre-web — каждый со своим трудностями и особенностями — Вы запускаете одно приложение. Всё, что описано ниже, живёт в одном аккаунте, на Вашем оборудовании, под Вашим контролем.

Файлы хранятся как обычные файлы в обычных папках на диске. Если вдруг понадобится переехать с DiscoDrive на другое хранилище, Вы просто откроете папку и найдёте свои данные ровно там, где их оставили. Никакого проприетарного хранилища фрагментами, никакой базы, которую невозможно прочитать, никакой привязки к одному поставщику (vendor lock-in).

Кроссплатформенность заложена в основу: Apple, Windows, Linux и Android.

**Скачать приложения:** сборки для десктопа (macOS/Windows/Linux) и Android — на [странице релизов](https://github.com/discodrive-cloud/discodrive-apps/releases/latest); приложение для iOS собирается из исходников. Все клиенты — в [discodrive-apps](https://github.com/discodrive-cloud/discodrive-apps).

---

## Что внутри

### 📁 Файлы и синхронизация

- Быстрый современный **файловый менеджер в браузере** — загрузка, скачивание, создание папок, переименование, перемещение, с перетаскиванием (drag-and-drop) файлов *и* папок.
- **Предпросмотр в один клик** — изображения, PDF, Markdown, код с подсветкой, документы Word и таблицы Excel открываются прямо в браузере, ничего не нужно скачивать.
- **Версии** — у каждого файла хранится недавняя история, можно вернуться к прежней версии после неудачной правки.
- **Корзина** — удалённые файлы попадают в корзину, откуда их можно восстановить.
- **Общий доступ** — публичные ссылки: с истечением срока, с паролем, только на чтение.
- **Подключается как диск** — открывайте хранилище прямо из Finder, Проводника Windows, файлового менеджера Linux или файлового приложения Android по WebDAV. Ничего устанавливать не нужно.
- **Синхронизация на компьютере** — лёгкое приложение держит реальную папку на компьютере синхронизированной с сервером, как Dropbox.
- **Данные не теряются** — если один и тот же файл изменился в двух местах сразу, сервер сохранит обе версии как конфликтные копии.

### 🔒 Зашифрованный сейф

- **Приватная папка со сквозным (E2E) шифрованием** для самых чувствительных файлов.
- Файлы шифруются **на Вашем устройстве** ещё до отправки — сервер хранит их только в зашифрованном виде.
- Открывается прямо в браузере или в клиентском приложении (расшифровка происходит локально), а если Вы забыли пароль — сейф разблокируется кодом восстановления.
- Построен на **открытом формате, совместимом с [Cryptomator](https://cryptomator.org)** — если понадобится открыть сейф отдельно или перейти на другой сервис, его можно расшифровать в Cryptomator или другими сторонними приложениями.

### 📅 Календарь, контакты и задачи

- Полноценные **календари, адресные книги и напоминания** — события, контакты и дела в одном месте.
- **Нативно на Apple** — Вы добавляете аккаунт в системных настройках iOS/macOS, и штатные Календарь, Контакты и Напоминания просто работают, без отдельного приложения.
- **На всём остальном** — работает с DAVx⁵ на Android и с Thunderbird, Evolution или eM Client на компьютере.
- Удобный **веб-интерфейс** для создания и редактирования событий, контактов и задач.
- **Делитесь календарём или адресной книгой** с близкими.

### 🎵 Музыка — Ваш личный стриминг

- Превращает Вашу коллекцию музыки в личный стриминговый сервис, доступный откуда угодно.
- **Работает с привычными приложениями** — любой OpenSubsonic-совместимый плеер (Amperfy, Feishin и многие другие) подключается без настройки, с текстами песен, очередью и поиском.
- **Интернет-радио** — добавляйте любимые станции и слушайте их через те же приложения.
- **Подкасты** — подписывайтесь на ленты, скачивайте и слушайте эпизоды, продолжайте с того места, где остановились, по закладкам.
- **Встроенный редактор тегов** — редактируйте названия, исполнителей, альбомы, жанры и обложки прямо в браузере: по одному треку или целой папкой сразу.

### ▶️ Плеер — музыка и видео прямо в браузере

- Нажмите на трек или фильм в файлах — и он заиграет сразу, без скачивания и сторонних программ.
- **Не мешает работать** — плеер живёт в аккуратной панели внизу экрана: музыка играет, а Вы продолжаете разбирать файлы.
- **Видеотеатр** — для видео интерфейс разворачивается в удобный полноэкранный режим просмотра.
- **Плавающее окно** — плеер можно открепить в отдельное окошко поверх других программ (картинка-в-картинке); оно переживает даже перезагрузку страницы.
- **10-полосный эквалайзер** с готовыми пресетами — подстройте звук под свои наушники или колонки.

### 📚 Книги — Ваша личная библиотека

- Личная библиотека для электронных книг и комиксов, готовая к чтению на любом устройстве.
- **Читается на любом устройстве** — каталог по стандарту OPDS, к которому напрямую подключаются KOReader, PocketBook, Marvin и другие приложения для чтения (а через браузер — вообще любое устройство).
- **Все нужные форматы** — EPUB, FB2, PDF, MOBI, CBZ и CBR, с миниатюрами обложек и поиском по всему каталогу.
- **Синхронизация прогресса чтения** — начните книгу на одном устройстве и продолжите ровно с того же места на другом.
- **Редактор метаданных** — редактируйте названия, авторов, серии, теги и описания в браузере: по одной книге или сразу по всей папке.

### 🔖 Сохранённое — закладки, статьи и закачки

Личный уголок для всего, что Вы нашли в интернете и не хотите потерять.

- **Закладки** — коллекция ссылок хранится в Вашем облаке и синхронизируется через браузерное расширение, а не заперта в одном браузере.
- **Читать позже** — сохраняйте статьи целиком: сервер сохранит чистый текст без рекламы и баннеров, а Вы прочитаете, когда будет время.
- **Закачки на сервер** — киньте ссылку на файл, и сервер скачает его сам, прямо в Ваше облако — даже когда Ваш компьютер выключен.

### 🛡️ Аккаунты и безопасность

- **Двухфакторная аутентификация** TOTP через любое приложение-аутентификатор плюс одноразовые резервные коды.
- **Passkeys** — вход по Face ID, Touch ID или аппаратному ключу, вообще без пароля.
- **Журнал безопасности**.
- **Уведомления по электронной почте** о важных событиях безопасности и аккаунта.
- **Встроенная защита от подбора паролей**.
- **Несколько пользователей** с персональными квотами.
- **Пароли для приложений** — чтобы подключать файловые, музыкальные и книжные приложения, не раскрывая основной пароль.
- **TLS везде** по умолчанию.

После смены пароля отзываются прежние сессии, refresh-токены устройств, DAV-пароли и ключи/пароли Subsonic, OPDS и KOReader. Устройства нужно привязать заново, а секреты интеграций — выпустить повторно. Зарегистрированные passkeys и настройка TOTP сохраняются. Миграция 000011 сохраняет существующие привязки до следующей смены пароля: если Вы меняли пароль для восстановления контроля до установки исправления, повторите смену после обновления. Начатые ранее MFA/WebAuthn-входы потребуется начать заново, а медиассылки — получить повторно.

### 🌍 Ваше, везде и на Вашем языке

- Совместимо с операционными системами Apple, Windows, Linux и Android.
- В данный момент доступны **7 языков** интерфейса: английский, немецкий, украинский, французский, испанский, русский и сербский.

---

## Установка

DiscoDrive — это один сервер плюс PostgreSQL. Перед ним можно поставить nginx — для TLS и быстрой отдачи файлов через X-Accel. Ниже два способа развернуть всё это. Большинству подойдёт **Вариант 1**.

> **Перед стартом необходимо подготовить два секретных ключа.** Сервер не запустится с временными значениями из `.env.example`. Сгенерируйте их один раз и держите неизменными:
>
> - `JWT_SECRET` — `openssl rand -base64 48`;
> - `SETTINGS_ENCRYPTION_KEY` — `openssl rand -hex 16`.
>
> И помните: приложениям Apple (CalDAV/CardDAV) нужен **HTTPS** — по незашифрованному HTTP они не работают.

### Вариант 1. Docker, с клонированием репозитория (рекомендуется)

Самый простой путь: одна команда `docker compose up` поднимает сервер, базу данных и nginx с TLS.

**Нужно:** git, Docker и Docker Compose.

```sh
git clone https://github.com/discodrive-cloud/discodrive.git
cd discodrive
cp .env.example .env
# отредактируйте .env: задайте JWT_SECRET, SETTINGS_ENCRYPTION_KEY, BASE_DOMAIN,
# POSTGRES_PASSWORD и, при желании, остальное
docker compose up -d
```

Получите одноразовый токен настройки через консоль сервера:

```sh
docker compose exec app cat /data/.bootstrap/setup-token
```

Откройте **https://server_address:8443** и укажите токен, почту и пароль администратора. Первоначальная настройка доступна только через HTTPS. Для уведомлений по почте используйте действующий адрес. Файл токена имеет права `0600`; в БД хранится только SHA-256-хеш. После настройки файл удаляется. Сам токен не выводится в журналы сервера.

Путь по умолчанию — `<STORAGE_ROOT>/.bootstrap/setup-token`; его можно переопределить через `SETUP_TOKEN_FILE`. Храните файл вне пользовательских деревьев, с доступом только у процесса сервера, на постоянном диске. Если токен незавершённой настройки потерян, удалите его файл через консоль и перезапустите сервер: новый токен заменит старый. Порт backend должен быть закрыт от клиентов: заголовок `X-Forwarded-Proto` должен устанавливать только доверенный reverse proxy.

Для существующих установок миграция сразу закрывает первоначальную настройку. Удаление последнего администратора не открывает её снова. Восстановление выполняется через консоль сервера/БД: восстановите резервную копию либо назначьте администратора из существующих аккаунтов (`UPDATE users SET role = 'admin' WHERE email = 'your-existing-account@example.com';`). Не сбрасывайте признак завершённой настройки ради восстановления доступа.

Как это устроено:

- Файлы хранятся в `/data`.
- TLS-сертификаты в `deploy/nginx/certs/` - Необходимо создать и положить **настоящий или самоподписанный сертификат** (как сгенерировать — в Варианте 2) Compose публикует только HTTPS; для стандартного порта задайте `NGINX_HTTPS_PORT=443`.

Чтобы остановить: `docker compose down` (данные сохранятся). Чтобы обновить после `git pull`: `docker compose up -d --build`.

### Вариант 2. Отдельными компонентами (сборка из исходного кода)

Если Вы не хотите использовать Docker — соберите исполняемый файл сами и запустите его рядом с обычным PostgreSQL. Исполняемый файл самодостаточен - в него включены все компоненты и составляющие.

**Для сборки необходимы:** Go 1.25+, Node.js 22+, PostgreSQL 16+.

**1. Соберите веб-интерфейс** (Nuxt → статические файлы в `web/dist`):

```sh
cd web
npm install
npm run generate
cd ..
```

**2. Соберите сервер** (исполняемый файл вберёт в себя `web/dist` и миграции):

```sh
CGO_ENABLED=0 go build -trimpath -o discodrive ./cmd/server
```

**3. Поднимите PostgreSQL** и создайте базу данных с пользователем:

```sh
createuser disco --pwprompt
createdb discodrive --owner disco
```

**4. Задайте переменные окружения** (полный список — в `.env.example`):

```sh
export DATABASE_URL="postgres://disco:ПАРОЛЬ@localhost:5432/discodrive?sslmode=disable"
export JWT_SECRET="$(openssl rand -base64 48)"
export SETTINGS_ENCRYPTION_KEY="$(openssl rand -hex 16)"
export BASE_DOMAIN="example.com"
export STORAGE_ROOT="/var/lib/discodrive/data"   # каталог должен существовать и быть доступен на запись
export APP_PORT="8080"
export XACCEL_ENABLED="false"                     # без nginx сервер раздаёт файлы сам
```

**5. Запустите**:

```sh
./discodrive
```

До первоначальной настройки подключите HTTPS reverse proxy и откройте его HTTPS-адрес. Через консоль сервера прочитайте токен командой `cat "$STORAGE_ROOT/.bootstrap/setup-token"` (либо по пути `SETUP_TOKEN_FILE`) и используйте его для создания администратора. Не открывайте клиентам HTTP-порт backend.

**nginx (необязательно).** Нужен для HTTPS и для быстрой отдачи файлов через X-Accel. Возьмите за основу `deploy/nginx/default.conf.template`, пропишите свои TLS-сертификаты и установите `XACCEL_ENABLED=true` — тогда сервер отдаёт файлы заголовком `X-Accel-Redirect`, а само тело файлов раздаёт nginx (расположение `/data/`, значение параметра `STORAGE_ROOT`).

**Обязательный HTTPS.** Публичный HTTP-порт больше не публикуется. HTTP-listener nginx отклоняет запросы, не передавая их приложению и не перенаправляя: во всех клиентах сразу укажите HTTPS-адрес. Редирект не защищает пароль, уже отправленный по HTTP. Включены TLS 1.2/1.3 и HSTS. В поставляемом шаблоне nginx отключён access log, поскольку некоторые поддерживаемые клиенты передают токены в query string.

При прямом запуске бинарник по умолчанию слушает `127.0.0.1:8080` и принимает подтверждение HTTPS только от непосредственного прокси из `TRUSTED_PROXY_CIDRS` (CIDR через запятую; по умолчанию `127.0.0.1/32,::1/128`). Прокси должен перезаписывать `X-Forwarded-Proto` фактической схемой соединения. В Compose приложение слушает интерфейс контейнера, доверяет приватным IPv4-адресам в изолированной сети и публикуется только HTTPS nginx. Для нестандартной сети задайте реальные адреса/подсеть прокси и держите backend-порт закрытым от клиентов. Если перед nginx стоит ещё один прокси, подключайте его к nginx по HTTPS; для сохранения исходного IP в rate limit отдельно настройте доверенные real-IP адреса.

Только для **локальной разработки** при прямом запуске можно задать `APP_HOST=127.0.0.1` (либо `::1`) и `ALLOW_INSECURE_HTTP=true`. На других интерфейсах сервер откажется запускаться в таком режиме. Первичная настройка администратора всё равно требует HTTPS. Compose принудительно отключает это исключение. При обновлении обновите/пересоздайте и app, и nginx; старые HTTP-адреса в клиентах замените на HTTPS.

**Границы хранилища.** Символические ссылки внутри `STORAGE_ROOT` не поддерживаются: сервер отклоняет их при чтении и записи и пропускает при пересканировании, включая ссылки на другого пользователя внутри того же хранилища. Библиотеки, организованные через симлинки, нужно заменить обычными файлами и каталогами. На Linux и macOS медиапарсеры, принимающие только путь, работают через закреплённые файловые дескрипторы; на Linux требуется доступный `/proc/self/fd`. При использовании X-Accel сохраняйте `disable_symlinks on;` в каждом nginx location `/__data/`: nginx открывает файл отдельно, уже после авторизации в Go. Эта защита закрывает обход через символические ссылки; она не изолирует данные от администратора с прямым доступом на запись, жёстких ссылок или bind mounts.

TLS-сертификаты возьмите у Let's Encrypt (`certbot`) или сгенерируйте самоподписанные:

```sh
openssl req -x509 -newkey rsa:2048 -nodes -days 825 \
  -keyout deploy/nginx/certs/dev-key.pem \
  -out deploy/nginx/certs/dev.pem -subj "/CN=localhost"
```

**Автозапуск через systemd (необязательно).** Пример модуля `/etc/systemd/system/discodrive.service`:

```ini
[Unit]
Description=DiscoDrive
After=network.target postgresql.service

[Service]
ExecStart=/usr/local/bin/discodrive
EnvironmentFile=/etc/discodrive.env
Restart=on-failure
User=discodrive

[Install]
WantedBy=multi-user.target
```

Переменные из шага 4 сложите в `/etc/discodrive.env` (формат `KEY=value`, по одной на строку), затем выполните:

```sh
sudo systemctl daemon-reload
sudo systemctl enable --now discodrive
```

---

## Лицензия и коммерческое использование

DiscoDrive распространяется как **source-available** под лицензией [PolyForm Noncommercial License 1.0.0](../LICENSE).

- ✅ **Бесплатно для любого некоммерческого использования** — разворачивайте для себя, семьи, для хобби, учёбы или экспериментов. Ради этого всё и сделано.
- ✅ **Изменяйте как угодно** — при условии сохранения обязательного указания авторства.
- ❌ **Коммерческое использование запрещено.**

Нужно коммерческое использование? Доступна отдельная коммерческая лицензия — напишите на [info@discodrive.app](mailto:info@discodrive.app).

---

## ❤️ Поддержать проект

DiscoDrive развивается своими силами, без инвестиций. Отзывы и предложения приветствуются — пишите на [info@discodrive.app](mailto:info@discodrive.app).

А если Вам нравится DiscoDrive и хочется, чтобы он рос и дальше — поддержите проект:

- **[Поставьте звезду на GitHub](https://github.com/discodrive-cloud/discodrive)** ⭐
- **[Boosty](https://boosty.to/kosmosoid/donate)**
- **[Ko-fi](https://ko-fi.com/kosmosoid)**
- **[Криптовалюта](https://nowpayments.io/donation?api_key=7P893T0-48B4KZH-Q6PXBBT-D79X9TK)** — через NOWPayments

Спасибо! Каждый вклад помогает проекту двигаться вперёд.
